name: Build MonarchModKit.exe

on:
  push:
    branches: [ main ]
  workflow_dispatch:   # also lets you trigger manually from the GitHub website

jobs:
  build:
    runs-on: windows-latest   # GitHub spins up a real Windows machine

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up MinGW
        uses: msys2/setup-msys2@v2
        with:
          msystem: UCRT64
          install: mingw-w64-ucrt-x86_64-gcc

      - name: Compile
        shell: msys2 {0}
        run: |
          mkdir -p dist
          gcc monarch.c \
            -o dist/MonarchModKit.exe \
            -O2 \
            -mwindows \
            -municode \
            -lcomctl32 \
            -lcomdlg32 \
            -lshell32 \
            -lole32 \
            -static-libgcc \
            -Wno-unused-parameter
          echo "Build complete:"
          ls -lh dist/MonarchModKit.exe

      - name: Upload .exe as artifact
        uses: actions/upload-artifact@v4
        with:
          name: MonarchModKit
          path: dist/MonarchModKit.exe
          retention-days: 90
